@model IEnumerable<Web.Models.ViewedPost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AnnouncementId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Seen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AnnouncementId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ViewedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ViewedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ViewedId })
        </td>
    </tr>
}

</table>
